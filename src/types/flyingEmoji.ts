type FlyingEmoji = {
    fromX: number;
    fromY: number;
    toX: number;
    toY: number;
    emoji: string;
};

// eslint-disable-next-line import/prefer-default-export
export type { FlyingEmoji };
